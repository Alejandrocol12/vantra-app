# Prisma CLI (local dev only — no subir a git)
DATABASE_URL="file:./prisma/dev.db"

# Turso — Runtime (requerido en producción Vercel)
TURSO_DATABASE_URL="libsql://vantra-db-[tu-usuario].turso.io"
TURSO_AUTH_TOKEN="tu-token-aqui"
